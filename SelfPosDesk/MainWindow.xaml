<Window x:Class="SelfPosDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SelfPosDesk"
        xmlns:vm="clr-namespace:SelfPosDesk.ViewModel"
        Title="Self POS" Height="600" Width="800">
    <Window.DataContext>
        <vm:SelfPosVM />
    </Window.DataContext>
    <Grid>
        <ContentControl Content="{Binding CurrentView}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

    </Grid>
</Window>
